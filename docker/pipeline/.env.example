# Example environment variables for pipeline container
# Copy this file to .env and fill in your Reddit API credentials

# Cron schedule (optional, defaults to hourly)
CRON_SCHEDULE=0 * * * *

# Reddit API credentials (required for fetch phase)
# OAuth script-type authentication only requires client_id, client_secret, and user_agent.
# IMPORTANT: PRAW requires lowercase 'praw_' prefix for environment variables
# Get these values from your praw.ini file
praw_client_id=your_client_id_here
praw_client_secret=your_client_secret_here
praw_user_agent=SOTD Pipeline
